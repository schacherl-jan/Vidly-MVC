@using Vidly.Models
@model Vidly.ViewModels.RandomMovieViewMoel

@{
    ViewBag.Title = "Random";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    var className = Model.Customers.Count > 1 ? "popular" : null;
}

<h2 class="@className">@Model.Movie.Name</h2>

@if (Model.Customers.Count == null)
{
    <text>Nikdo si nepujcil tento film</text>
}
<ul>
    @foreach (var customer in Model.Customers)
    {
        <li>@customer.Name</li>
    }
</ul>
@* Nepouzivat varianty pres string *@
@*<h2>@ViewData["Movie"].</h2>*@

@*<h2>@(((Movie)ViewData["Movie"]).Name)</h2>*@

@*<h2>@ViewBag.Movie</h2>*@